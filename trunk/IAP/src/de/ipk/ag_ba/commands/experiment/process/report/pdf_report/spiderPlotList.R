# TODO: Add comment
# 
# Author: Entzian
###############################################################################
### section start with: 30

#spiderOptions= list(typOfGeomBar=c("x", "y"))
spiderOptions= list(typOfGeomBar=c("x"))

spiderPlotList <- list(
		"side.height.norm (mm)$side.width.norm (mm)$side.area.norm (mm^2)$top.area.norm (mm^2)$side.fluo.intensity.average (relative)$side.nir.intensity.average (relative)$side.vis.hue.average$top.vis.hue.average" = list(
				plotName = "height (zoom corrected) (mm)$width (zoom corrected) (mm)$side area (zoom corrected) (mm^2)$top area (zoom corrected) (mm^2)$side fluo intensity$side nir intensity$side visible hue average value$top visible hue average value", 
				section = 30,
				subsection = 1)
#	,
#		
#	"side.height (px)$side.width (px)$side.area (px)$top.area (px)$side.fluo.intensity.average (relative)$side.nir.intensity.average (relative)$side.vis.hsv.h.average$top.vis.hsv.h.average" = list(
#			plotName =  "height (px)$width (px)$side area (px)$top area (px)$side fluo intensity$side nir intensity$side visible hue average value$top visible hue average value", 
#			section = 30,
#			subsection = 2)
		)


