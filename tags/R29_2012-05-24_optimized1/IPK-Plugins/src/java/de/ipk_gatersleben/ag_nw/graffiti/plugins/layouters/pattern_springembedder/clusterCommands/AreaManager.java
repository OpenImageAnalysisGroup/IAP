package de.ipk_gatersleben.ag_nw.graffiti.plugins.layouters.pattern_springembedder.clusterCommands;

import org.graffiti.graph.Graph;

public class AreaManager {
	
	public AreaManager(Graph g) {
		// TODO Auto-generated constructor stub
	}
	
	public void claimNodePositions() {
		// Vector2d maxXY = NodeTools.getMaximumXY(getNodeListFromEdgeList(workEdges), 1, 0, 0, true);
		// Vector2d minXY = NodeTools.getMinimumXY(getNodeListFromEdgeList(workEdges), 1, 0, 0, true);
		// int x = (int)((maxXY.x-minXY.x)/20d);
		// int y = (int)((maxXY.y-minXY.y)/20d);
		
	}
	
}
